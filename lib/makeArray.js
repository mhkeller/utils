/**
 * Make an array of n elements
 * @param {number} n - The number of elements
 * @returns {Array} - An array of n elements
 */
export default function makeArray (n) {
	return Array.from(Array(n))
}
